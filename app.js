const add = function (a, b) {
  return a + b;
};

const subtract = function (a, b) {
  return a - b;
};

const multiply = function (arr) {
  return multiply(arr.reduce((a, b) => a * b, 0));
};

const divide = function (a, b) {
  return a / b;
};

const operate = function () {};
